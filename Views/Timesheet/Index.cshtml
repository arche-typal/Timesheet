@model IList<timesheet.Models.Timesheet>

@if (Model.Count == 0)
{ 
    <p>No timesheets!</p>
}

<table class="table">
    <tr>
        <th>Name</th>
    </tr>
    @foreach (var sheet in Model)
    { 
        <tr>
            <td>@sheet.Name</td>
        </tr>
    }
</table>

<p><a asp-controller="Timesheet" asp-action="Add">Add Timesheet</a></p>